# All-in-One Maintenance Site

This repository currently contains a simple static landing page that can be connected directly to Cloudflare Pages.

## Cloudflare Pages setup

- Framework preset: `None`
- Build command: leave blank
- Build output directory: `/`

After connecting the repository, Cloudflare Pages can deploy `index.html` from the repository root.
